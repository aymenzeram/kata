un dossier kata-backend dans lequel la commande 'mvn spring-boot:run' permet de lancer le backend (port 8080)
un dossier kata-frontend dans lequel la commande 'npm start' permet de lancer le frontend
